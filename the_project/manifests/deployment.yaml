apiVersion: apps/v1
kind: Deployment
metadata:
  name: dwk-environments
  namespace: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dwk-environments
  template:
    metadata:
      labels:
        app: dwk-environments
    spec:
      volumes:
        - name: img-storage
          persistentVolumeClaim:
            claimName: img-claim
      containers:
        - name: theproject-container
          image: PROJECT/IMAGE
          env:
            - name: IMAGE_SOURCE
              valueFrom:
                configMapKeyRef:
                  name: project-configmap
                  key: image-source
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: project-configmap
                  key: port
            - name: DB
              valueFrom:
                configMapKeyRef:
                  name: project-configmap
                  key: db
            - name: USER
              valueFrom:
                configMapKeyRef:
                  name: project-configmap
                  key: user
            - name: HOST
              valueFrom:
                configMapKeyRef:
                  name: project-configmap
                  key: host
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: project-configmap
                  key: db-port
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: SECRET 
          volumeMounts:
            - name: img-storage
              mountPath: /usr/src/app/files